export default class User {

    constructor(id, avatar, userName, birthday, subscribe) {
        this.id = id
        this.avatar = avatar
        this.userName = userName
        this.birthday = birthday
        this.subscribe = subscribe
    }
}